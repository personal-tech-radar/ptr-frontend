/var/log/nginx/*.log {
  daily
  rotate 14
  maxsize 25M
  missingok
  notifempty
  compress
  delaycompress
  sharedscripts
  postrotate
    test ! -f /run/nginx/nginx.pid || kill -USR1 $(cat /run/nginx/nginx.pid)
  endscript
}
